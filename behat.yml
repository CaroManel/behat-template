default:
  suites:
    default:
      path: %paths.base%/features
      contexts: 
        - FeatureContext
  extensions:
    Behat\MinkExtension:
      base_url: http://test.dev/
      sessions:
        default:
          goutte: ~
